overwrite: true
schema: "schema/index.graphql"
documents: null
generates:
  graphqlTypes.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      namingConvention:
      enumValues: keep
